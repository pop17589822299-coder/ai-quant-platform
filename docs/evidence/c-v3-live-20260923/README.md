# C：V3 真实行情与真实 DeepSeek 运行测试

被测生产 SHA：`a6c939351579076ce65b0fec6573bf72cb3cfcd9`。
测试时间：2026-09-23 00:10—00:17，Asia/Shanghai。
结论：**C 的实时 MA/MACD 数值链路与真实 DeepSeek 回测解读通过；全实时项目链路未全部通过。搜索与默认综合分析仍受数据源故障阻塞。**

## 环境和隔离

- 未修改生产源码、量化算法、API、公共依赖或原基线。
- Python 3.12.10 / pandas 2.3.3 / NumPy 2.5.3；本机 MySQL 8.0.31，新建唯一命名隔离库，schema v9。未复用静态预跑数据库或写入业务 ai_quant。
- 正式 `create_app()`，dependency_overrides 为空，正式 Provider、行情服务、路由、量化、仓库和 LLM 客户端。外部启动器只配置进程环境、新库及 live 标识/请求日志。
- 前端 5185 → 后端 8125，所有 mock 开关关闭，页面标识 LIVE；静态 5184 环境单独保留。
- 真实模型配置为 `deepseek-flash`，官方配置地址 api.deepseek.com；凭据未输出/提交。连通性 JSON 探针通过。

## 实时取数与回测

三只股票 600519、000001、300750 均经正式行情入口成功获取 2025-07-04 至 2026-09-22 日线，各 299 行；此前已清空的新库不含静态包。
数据状态显示 `mode=live`，实际使用项目已实现的腾讯 qfq 备用端点 `https://web.ifzq.gtimg.cn/appstock/app/fqkline/get`。这不是本轮修改 Provider 或人为替换样本，也不是东财源全恢复。

| 股票 | 默认策略 | 回测 ID | 收益率 | 成交 / 往返 | 曲线 |
|---|---|---:|---:|---:|---:|
| 600519 | MA 5/20 | 3 | -12.5375366188% | 20 / 10 | 299 |
| 600519 | MACD 12/26/9 | 4 | -17.0739084387% | 28 / 14 | 299 |
| 000001 | MA 5/20 | 5 | -4.8385738616% | 22 / 11 | 299 |
| 000001 | MACD 12/26/9 | 6 | -0.7556019678% | 26 / 13 | 299 |
| 300750 | MA 5/20 | 7 | +7.5448715223% | 18 / 9 | 299 |
| 300750 | MACD 12/26/9 | 8 | +23.1758712949% | 26 / 13 | 299 |

六组 POST 均成功，按保存输入快照重新直接计算，完整 C 结果与 GET 的 c_result、新建连接直接读 MySQL c_result_text 一致，包括类型、哈希、订单、曲线。POST 中对应数值字段相同。

MA 的外层 effective_parameters 按已有 B 契约为五个可配置参数；完整 C 配置保存在 c_result。初版临时验收驱动把这两个不同层次全等比较而触发断言，确认唯一差异是该已有封装后，仅修正本轮外部驱动的比较口径，另外逐项断言五项参数与 C 一致。MACD 外层及 C 同为六参数，仍全等。生产实现及任何算法、冻结测试均未修改。

## 真实 AI 与页面

- 页面实际进入 MA 回测 #3，“AI 解读本回测”成功调用真实 deepseek-flash，产生报告 #1。
- 报告事实卡显示原始保存指标：总收益 -12.54%、回撤 -21.86%、20 笔订单/10 次往返。模型解释包含保存参数、历史区间和简化成交假设，不是上轮测试占位文案。
- 连续两次 GET 报告一致；另建 MySQL 引擎/会话逐字段核对正文、上下文、哈希、analysis_mode=custom_backtest、backtest_id=3、model_name，均通过。
- 实际点选导出，Downloads 生成 6260 字节 Markdown，见 `real-ai-export.md`。
- 工作台 K 线、评分及自动 V1 回测可显示，但基本信息请求仍有 502；不能因为 600519 页头有名称（前端既有回退机制）就声称基本信息源稳定。

## 未通过项与归属

1. **B/D：目录同步阻塞搜索。** POST /stocks/catalog/sync 两次 HTTP 502 / 50001；GET /stocks/search?keyword=600519 返回 HTTP 503 / 50006。data-status 记录 primary TimeoutError，delayed host catalog HTTP 502。新库目录确实为空，未用旧目录或三股固定名单伪装全量同步成功。
2. **B/D：默认综合分析失败。** 页面“开始 AI 分析”返回 HTTP 502，独立重试 POST /ai/analyze {stock_code:600519} 再得 502 / 50001；未保存默认报告。股票基本信息 600519 首次成功、随后多次失败，000001/300750 失败。真实 LLM 连接及 custom 解读均通过，故不能把该失败归为 LLM 配置错误；具体默认上下文中的失败依赖仍需 B/D 排查。
3. **B/D：报告溯源需确认。** data-status 明确 live + 腾讯 URL，但生成的 custom 报告仍 source_mode/provider=unknown，页面与导出均展示“未知来源”。C 原样保留，不自行补写来源或改快照。

本轮不代替 B 的 V1→v9 迁移链最终签字，也不把上述部分通过宣布为全实时验收通过。

## 证据与复核

`backtest-summary.json` 保存六组完整结果 canonical SHA-256；完整 POST 原文和日线响应保留在本地 `.codex-task/v3-live-20260923/`，避免重复提交大量行情行。
本目录提供实际执行的探针、各失败响应、行情来源/状态、真实报告与 MySQL 回读结论。`FILES_SHA256.json` 对 Git blob 字节校验（本地 CRLF 转换可能改变字节）。

该测试只验证真实数据接入与计算/保存/展示，不证明策略实盘盈利能力。
